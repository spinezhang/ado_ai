# Environment variables for ADO AI Web Service
# Copy this file to .env and fill in your values

# Encryption master key for credential storage (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
# If not set, a temporary key will be generated automatically (not recommended for production)
ENCRYPTION_MASTER_KEY=your-base64-encoded-32-byte-key-here

# Database URL (defaults to SQLite in current directory)
DATABASE_URL=sqlite:///./ado_ai.db

# Optional: Anthropic API key (can also be set in server_config.json or provided during setup)
# ANTHROPIC_API_KEY=your-api-key-here

# Optional: Default work folder path
# DEFAULT_WORK_FOLDER=/path/to/workspace

# Server configuration (optional)
# HOST=0.0.0.0
# PORT=8000
